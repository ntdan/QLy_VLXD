<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cuahang.qly_vlxd">

    <application
        android:allowBackup="true"
        android:icon="@drawable/drill"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Customer_UnCompletedPayment"
            android:label="@string/title_activity_customer__un_completed_payment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CreateInvoice" />
        <activity
            android:name=".NewProduct"
            android:label="@string/title_activity_new_product"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProductList">
        </activity>
        <activity android:name=".CustomerList" />
        <activity android:name=".NewCustomer"></activity>
    </application>

</manifest>